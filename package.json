{
  "name": "tokensdrift-monorepo",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "test": "npm run test --workspace packages/cli",
    "build": "npm run build --workspace packages/cli",
    "typecheck": "npm run typecheck --workspace packages/cli"
  },
  "devDependencies": {
    "@types/node": "^20.14.10",
    "esbuild": "^0.23.0",
    "tsx": "^4.16.2",
    "typescript": "^5.5.4"
  }
}
